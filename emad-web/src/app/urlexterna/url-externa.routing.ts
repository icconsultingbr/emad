import { Routes } from '@angular/router';
import { UrlExternaComponent } from './url-externa.component';

export const urlExternaRoutes: Routes = [
    {
        path: '',
        component: UrlExternaComponent,
    }
];
